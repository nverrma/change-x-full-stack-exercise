<p style="color: green"><%= notice %></p>

<h1>Comments for <%= @applicant.name.capitalize %></h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <%= render comment %>
  <% end %>
</div>
